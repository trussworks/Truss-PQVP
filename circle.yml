machine:
  services:
    - docker
  environment:
    AWS_DEFAULT_REGION: us-west-2

dependencies:
  override:
    - sudo wget https://releases.hashicorp.com/terraform/0.8.6/terraform_0.8.6_linux_amd64.zip -O /usr/local/bin/terraform.zip
    - cd /usr/local/bin && sudo unzip terraform.zip
    - terraform version
    - eval $(aws ecr get-login --region $AWS_DEFAULT_REGION)
    - docker build --rm=false -t pqvp-demo:latest .

test:
  override:
    - terraform/scripts/run_tests.sh

deployment:
  prod:
    branch: mk-fix-push
    commands:
      - docker tag pqvp-demo:latest ${AWS_ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/pqvp-demo:latest
      - docker push ${AWS_ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/pqvp-demo:latest
